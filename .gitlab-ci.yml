# GitLab CI/CD pipeline for kahl CLI (Rust)
#
# Pipeline structure:
#
#   build ──→ test:integration
#     │              │
#     │    test:unit─┤
#     │         │    │
#     │    lint:fmt──┤
#     │         │    │
#     │    lint:clippy
#     │
#     Releases are built locally via `release` script (cross-compilation).

stages:
  - build
  - test
  

variables:
  GIT_DEPTH: 1
  RUST_VERSION: "1.93"
  CARGO_HOME: ${CI_PROJECT_DIR}/.cargo
  # Disable secrets filter in CI (testing secrets filter itself)
  CLAUDE_CODE_SHELL_PREFIX: ""

# ====================================================================================
# Build Stage
# ====================================================================================

build:
  stage: build
  image: rust:${RUST_VERSION}-alpine
  script:
    - apk add --no-cache musl-dev yq bash
    - rustup target add x86_64-unknown-linux-musl
    - ./prebuild.sh
    - cargo build --release --target x86_64-unknown-linux-musl
    - mkdir -p build && cp target/x86_64-unknown-linux-musl/release/kahl build/kahl
    - ./build/kahl --version
  artifacts:
    paths:
      - build/kahl
    expire_in: 1 day
  cache:
    key: cargo-build-${CI_COMMIT_REF_SLUG}
    paths:
      - .cargo/registry
      - .cargo/git
      - target
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+$/
      when: never  # Tag releases handled separately
    - if: $CI_PIPELINE_SOURCE == "push"
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"

# ====================================================================================
# Test Stage
# ====================================================================================

test:unit:
  stage: test
  image: rust:${RUST_VERSION}-slim
  before_script:
    - apt-get update -qq && apt-get install -y -qq wget
    - wget -q "https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64" -O /usr/local/bin/yq && chmod +x /usr/local/bin/yq
  script:
    - ./prebuild.sh
    - cargo test -- --nocapture
  cache:
    key: cargo-test-unit-${CI_COMMIT_REF_SLUG}
    paths:
      - .cargo/registry
      - .cargo/git
      - target
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+$/
      when: never
    - if: $CI_PIPELINE_SOURCE == "push"
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"

lint:fmt:
  stage: test
  image: rust:${RUST_VERSION}-slim
  before_script:
    - apt-get update -qq && apt-get install -y -qq wget
    - wget -q "https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64" -O /usr/local/bin/yq && chmod +x /usr/local/bin/yq
    - rustup component add rustfmt
  script:
    - ./prebuild.sh
    - cargo fmt --check
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+$/
      when: never
    - if: $CI_PIPELINE_SOURCE == "push"
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"

lint:clippy:
  stage: test
  image: rust:${RUST_VERSION}-slim
  before_script:
    - apt-get update -qq && apt-get install -y -qq wget
    - wget -q "https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64" -O /usr/local/bin/yq && chmod +x /usr/local/bin/yq
  script:
    - ./prebuild.sh
    - rustup component add clippy
    - cargo clippy --all-targets -- -D warnings
  cache:
    key: cargo-clippy-${CI_COMMIT_REF_SLUG}
    paths:
      - .cargo/registry
      - .cargo/git
      - target
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+$/
      when: never
    - if: $CI_PIPELINE_SOURCE == "push"
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"

test:integration:
  stage: test
  needs: [build]
  image: debian:bookworm-slim
  script:
    - ./build/kahl --version
    - ./test.sh -v
  artifacts:
    when: on_failure
    paths:
      - tests/*.log
    expire_in: 1 week
  rules:
    - if: $CI_COMMIT_TAG =~ /^v\d+\.\d+\.\d+$/
      when: never
    - if: $CI_PIPELINE_SOURCE == "push"
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
